<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{notificationTitle}} - {{appName}}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #6f42c1; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background: #f8f9fa; }
        .notification-box { background: white; padding: 15px; border-left: 4px solid #6f42c1; margin: 15px 0; }
        .button { display: inline-block; padding: 12px 24px; background: #6f42c1; color: white; text-decoration: none; border-radius: 4px; margin: 15px 0; }
        .footer { text-align: center; padding: 20px; font-size: 12px; color: #666; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{appName}}</h1>
            <p>{{notificationTitle}}</p>
        </div>
        
        <div class="content">
            <p>Hi {{recipientName}},</p>
            
            <div class="notification-box">
                <p>{{notificationMessage}}</p>
            </div>
            
            {{#if actionUrl}}
            <a href="{{actionUrl}}" class="button">View Details</a>
            {{/if}}
            
            <p>You can manage your notification preferences in your account settings.</p>
        </div>
        
        <div class="footer">
            <p>This email was sent by {{appName}}. If you don't want to receive these emails, you can <a href="{{unsubscribeUrl}}">unsubscribe</a>.</p>
        </div>
    </div>
</body>
</html>