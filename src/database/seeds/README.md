# Database Seeding

Place your seed scripts in this directory.

## Running Seeds
Create a script in `package.json` to run specific seeds or use a seeding library like `typeorm-extension` or `nestjs-seeder`.

Example structure:
- `create-users.seed.ts`
- `create-settings.seed.ts`
